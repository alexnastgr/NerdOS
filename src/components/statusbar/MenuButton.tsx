import { Menu } from "@/assets/icons/Huge";

export default function MenuButton() {

  return (
    <div className="menubtn bg-gradient-to-tr from-primary to-secondary">
      <Menu width={32} />
    </div>
  );
}
